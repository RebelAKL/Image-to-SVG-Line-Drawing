[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --strict-markers --tb=short

# configure markers
markers =
    edge: marks tests as edge case tests
    integration: marks integration tests
    slow: marks slow tests
    network: marks network tests
